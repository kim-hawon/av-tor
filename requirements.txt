# 상태기계 / 설정 (필수)
PyYAML>=6.0

# 음성/오디오 (스피커 재생, 음성 raw 녹음, STT)
#   없어도 SIM 모드로 동작하지만, 실제 재생/녹음하려면 필요
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.21

# 음성인식 STT (config 의 dummy.use_real_voice=true 일 때만 필요)
vosk>=0.3.45

# ── 라즈베리파이 전용 (맥/PC 에는 설치 불가, 설치 안 해도 SIM 으로 폴백) ──
# GPIO 제어 (LED/부저/진동/터치센서)
# RPi.GPIO>=0.7.1
# I2C 캐릭터 LCD (20x4)
# RPLCD>=1.3.1
# smbus2>=0.4.2
# 카메라 프레임 캡쳐 (gaze)
# opencv-python>=4.5
