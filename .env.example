# av-tor 환경변수 템플릿.
# 사용법: 이 파일을 ".env" 로 복사한 뒤 본인 값으로 채운다.
#   cp .env.example .env
# .env 는 .gitignore 로 보호되어 깃에 올라가지 않는다.

# ─── 텔레그램 알림 ───
# BotFather 로 받은 봇 토큰
TG_BOT_TOKEN=

# 메시지 받을 채팅 ID (개인 채팅이면 본인 user_id)
TG_CHAT_ID=
